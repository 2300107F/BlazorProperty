@page "/Account/Lockout"

<PageTitle>Account Locked Out - SG Realty Listings</PageTitle>

<div class="lockout-container">
    <div class="lockout-card">
        <div class="lockout-icon">
            <i class="fas fa-lock"></i>
        </div>
        <h1 class="lockout-title">Account Locked Out</h1>
        <p class="lockout-message">
            Your account has been temporarily locked due to multiple failed login attempts. Please try again later or contact support for assistance.
        </p>
        <div class="lockout-actions">
            <a href="/" class="home-button">Return to Home</a>
            <a href="/Account/Login" class="login-button">Try Again Later</a>
        </div>
    </div>
</div>

<style>
    /* Global Styles */
    :root {
        --primary-color: #1a365d;
        --accent-color: #d4af37;
        --text-light: #f8f9fa;
        --text-dark: #2d3748;
        --error-color: #dc3545;
    }

    .lockout-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        padding: 2rem;
        background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    }

    .lockout-card {
        background: white;
        border-radius: 20px;
        padding: 3rem;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 500px;
        width: 100%;
    }

    .lockout-icon {
        font-size: 4rem;
        color: var(--error-color);
        margin-bottom: 1.5rem;
    }

    .lockout-title {
        font-size: 2rem;
        color: var(--primary-color);
        margin-bottom: 1rem;
    }

    .lockout-message {
        font-size: 1.1rem;
        color: #6c757d;
        margin-bottom: 2rem;
    }

    .lockout-actions {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        margin-top: 2rem;
    }

    .home-button,
    .login-button {
        padding: 0.8rem 2rem;
        border-radius: 8px;
        text-decoration: none;
        font-size: 1rem;
        font-weight: 500;
        transition: background 0.3s ease;
        text-align: center;
    }

    .home-button {
        background: var(--primary-color);
        color: var(--text-light);
    }

    .home-button:hover {
        background: #12243d;
    }

    .login-button {
        background: var(--error-color);
        color: var(--text-light);
    }

    .login-button:hover {
        background: #c82333;
    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .lockout-card {
            padding: 2rem;
        }

        .lockout-title {
            font-size: 1.8rem;
        }

        .lockout-message {
            font-size: 1rem;
        }
    }
</style>
